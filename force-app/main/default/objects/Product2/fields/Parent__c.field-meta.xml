<?xml version="1.0" encoding="UTF-8" ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Parent__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description
  >DEPRECATED 2026-07-16 — superseded by BundleComponent__c. A single lookup could only ever put a product in one bundle and could not carry a per-bundle spread price. Existing values were migrated to the junction on 2026-07-16 and nothing writes this field any more. Kept for review before deletion.</description>
    <label>Parent</label>
    <referenceTo>Product2</referenceTo>
    <relationshipLabel>Products</relationshipLabel>
    <relationshipName>Parent</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
