<?xml version="1.0" encoding="UTF-8" ?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <deploymentStatus>Deployed</deploymentStatus>
    <description
  >Junction between a Bundle (Product2, RecordType=Bundle) and a component Product (Product2, RecordType=Item). Carries the spread price, which is the share of the bundle's fixed selling price attributed to this component. The spread lives here rather than on Product2 so the same product can sit in several bundles at different spreads.</description>
    <enableActivities>false</enableActivities>
    <enableHistory>false</enableHistory>
    <enableReports>true</enableReports>
    <enableSearch>true</enableSearch>
    <label>Bundle Component</label>
    <nameField>
        <displayFormat>BC-{0000}</displayFormat>
        <label>Bundle Component No.</label>
        <type>AutoNumber</type>
    </nameField>
    <pluralLabel>Bundle Components</pluralLabel>
    <sharingModel>ReadWrite</sharingModel>
    <visibility>Public</visibility>
</CustomObject>
