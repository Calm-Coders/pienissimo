<?xml version="1.0" encoding="UTF-8" ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Spread_Price__c</fullName>
    <description
  >The share of the bundle's fixed selling price attributed to this component LINE, quantity included (see Unit_Spread__c for the per-unit figure). Set per bundle at configuration time, so one product can carry a different spread in each bundle it belongs to. Product-level revenue statistics are computed from this value, never from the product's list price.</description>
    <label>Spread Price</label>
    <precision>16</precision>
    <required>true</required>
    <scale>2</scale>
    <trackTrending>false</trackTrending>
    <type>Currency</type>
</CustomField>
